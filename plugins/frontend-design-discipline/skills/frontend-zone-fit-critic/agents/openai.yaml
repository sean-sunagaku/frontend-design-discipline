interface:
  display_name: "Frontend Zone Fit Critic"
  short_description: "Catch overlap, cramped spacing, and weak local fit"
  default_prompt: "Use $frontend-zone-fit-critic to independently review the current section, zone, or cluster crop in this thread for overlap, visual kissing, breathing room, grouping quality, unnatural line breaks, local text fit, width allocation, and headings that should be 1 line or at most 2 lines but drift into a preventable third line, and return only PASS/FAIL plus remaining issues."
