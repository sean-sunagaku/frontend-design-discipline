interface:
  display_name: "Frontend Structure Auditor"
  short_description: "Audit frames, containment, and overflow"
  default_prompt: "Use $frontend-structure-auditor to review this artifact or build packet for frame hierarchy, containment, auto layout, overflow, bare text nodes, meaningful line breaks, and perimeter padding before build or signoff."
